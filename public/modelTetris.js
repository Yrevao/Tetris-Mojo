// board dimensions
const maxX = 10
const maxY = 40
const viewHeight = 20 // height the player actually sees

// game state
var board = newGrid(maxX, maxY)

// place a tetromino on the board
const placeBlock = (tet, x, y, rot) => {

}